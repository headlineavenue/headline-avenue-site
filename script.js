document.addEventListener("DOMContentLoaded",()=>{const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;const items=document.querySelectorAll(".reveal");if(reduced){items.forEach(el=>el.classList.add("in"))}else{const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("in");io.unobserve(entry.target)}}),{threshold:.12});items.forEach(el=>io.observe(el))}const toggle=document.querySelector(".menu-toggle");const nav=document.querySelector("header nav");if(toggle&&nav){toggle.addEventListener("click",()=>{const open=toggle.getAttribute("aria-expanded")==="true";toggle.setAttribute("aria-expanded",String(!open))});nav.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>toggle.setAttribute("aria-expanded","false")))}const progress=document.querySelector(".progress span");const update=()=>{if(!progress)return;const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h>0?scrollY/h*100:0)+"%"};update();addEventListener("scroll",update,{passive:true});document.querySelectorAll("[data-year]").forEach(el=>el.textContent=new Date().getFullYear())});